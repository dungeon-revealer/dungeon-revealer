sudo: false
language: node_js
node_js:
  - "node"


jobs:
  include:
    - stage: eslint
      script:
        - npm run eslint
    - stage: build
      script:
        - npm run build
    - stage: docker
      script:
        - docker build -t dungeon-revealer .

services:
  - docker
